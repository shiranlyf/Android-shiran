<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical" >

    <!-- 顶部 -->

    <LinearLayout
        android:id="@+id/car_address_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/top_bg_n"
        android:gravity="center_vertical"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/car_address_goback"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/btn_activate_bg"
            android:drawableLeft="@drawable/back"
            android:gravity="center_vertical"
            android:text="@string/person_order_address"
            android:textColor="@color/white"
            android:textSize="18sp" />
    </LinearLayout>

    <!-- 中间部分订单列表 -->

    <ListView
        android:id="@+id/person_address_listview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical" >
    </ListView>
    

    <!-- 底部部分 -->

    <LinearLayout
        android:id="@+id/person_address_botton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#ffffffff"
        android:gravity="center_horizontal"
        android:orientation="horizontal"
        android:padding="5dp" >


        <TextView
            android:id="@+id/btn_add_order_address"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp"
            android:background="@color/white"
            android:drawableLeft="@drawable/add_person_order_address"
            android:drawablePadding="5dp"
            android:gravity="center_vertical"
            android:text="@string/btn_add_order_address"
            android:textColor="#ffff5a00"
            android:textSize="16sp" />
    </LinearLayout>
</LinearLayout>